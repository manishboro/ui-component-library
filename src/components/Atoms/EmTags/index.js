import React from 'react';
import PropTypes from "prop-types";
import "./index.scss";

const EmTags = ({
}) => {

  return (
    <>
      <h1>sdsd</h1>
    </>
  );
};

EmTags.propTypes = {
  name: PropTypes.string,
};

export default EmTags;