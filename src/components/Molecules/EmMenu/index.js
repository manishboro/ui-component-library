import React from 'react';
import PropTypes from "prop-types";
import "./index.scss";

const EmMenu = ({
}) => {

  return (
    <></>
  );
};

EmMenu.propTypes = {
  name: PropTypes.string,
};

export default EmMenu;