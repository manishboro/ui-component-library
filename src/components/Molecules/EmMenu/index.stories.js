
import React from 'react';
import EmMenu from '.';

export default {
  title: 'Molecules/Menu',
  component: EmMenu,
};

const Template = (args) => <EmMenu {...args} />;

export const Menu = Template.bind({});
Menu.args = {
};
