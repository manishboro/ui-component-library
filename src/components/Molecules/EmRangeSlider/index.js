import React from 'react';
import PropTypes from "prop-types";
import "./index.scss";

const EmRangeSlider = ({
}) => {

  return (
    <></>
  );
};

EmRangeSlider.propTypes = {
  name: PropTypes.string,
};

export default EmRangeSlider;